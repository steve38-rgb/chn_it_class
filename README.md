# 資科補充 - 教學入口網

這是一個專為資訊科技課堂設計的入口網站，旨在提供學生快速存取常用連結、了解課堂規定以及掌握最新資訊動態。

## 🌐 網站特色
- **現代化設計**：高品質視覺效果，支援響應式佈局（手機/平板/電腦）。
- **快速連結**：集中收錄 Google Workspace, 學務系統, OpenID 等常用平台。
- **直覺導航**：分類清晰的課堂規定與活動資訊。

## 🚀 部署到 GitHub Pages
1. 在 GitHub 上建立一個新的儲存庫 (Repository)。
2. 將此目錄下的所有檔案上傳至儲存庫。
3. 進入儲存庫的 **Settings > Pages**。
4. 在 **Build and deployment** 下選擇 `Deploy from a branch`。
5. 選擇 `main` 分支並點擊 **Save**。
6. 您的網站將在幾分鐘後於 `https://[你的帳號].github.io/[儲存庫名稱]/` 上線。

## 📅 維護說明
- **更新內容**：直接編輯 `index.html` 中的對應區塊即可。
- **調整樣式**：修改 `style.css` 中的 CSS 變數或樣式規則。

## 📝 更新紀錄 (Update Log)
### [2026-01-29]
- **新增目錄結構**：建立 `grade1`, `grade2`, `grade3`, `tools` 資料夾。
- **預設頁面製作**：在各子目錄中建立 `index.html`，具備與首頁一致的設計風格並支援響應式導覽。
- **導覽列聯動**：更新首頁導覽連結，實現全站頁面跳轉功能。
- **JS 整合**：全站統一引用 `main.js`，確保滾動效果與手機版選單功能正常。

### [2026-01-29] (第二次更新)
- **搬移近期活動**：將「全國法規資料庫競賽」移至「過往活動資源」中，並優化了活動列表的視覺呈現（改為卡片式佈局）。
- **實作全站導覽列同步 (SPA 架構)**：
    - 修改 `main.js` 使用 `fetch` API 進行非同步頁面加載。
    - 點擊導覽連結時，僅會更換下方的內容區域（`#page-wrapper`），頂部的導覽列將保持不變且不重新載入。
    - 支援瀏覽器前進/後退歷史紀錄。
- **補全導覽連結**：恢復並新增了「好用連結」與「特別課程」的導覽選項。
- **統一頁面結構**：更新全站（一二三年級、小工具）的 HTML 結構，以符合動態加載需求。

---
*Powered by Antigravity*
